# Schemas

Runtime validation and shared contract types.

- `domain.ts`: session/template/image domain models.
- `api.ts`: endpoint request/response envelopes and stable error codes.
- `telemetry.ts`: funnel event and provider log payload schemas.
