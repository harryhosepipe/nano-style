# API

Server endpoints and request handlers.

- `contracts.ts`: canonical endpoint paths, session cookie constants, and request-id header key.
- `errors.ts`: stable API error type and safe client message mapping.
- `http.ts`: shared request parsing + JSON success/error response helpers.
